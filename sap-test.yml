--- 
- hosts: "{{ hname1 }}"
  become: yes
  become_method: runas
  become_user: system
  gather_facts: false
  vars:
    acc_id: "sgi"
    trans_num: "{{ tower_job_id }}"
  roles:
    - role: sap-hostagent-update-windows
      ignore_errors: yes  
      