---
- name: Print the sid_adm variable value using ansible
  hosts: "{{ sap_server_name }}"
  become: yes
  become_method: enable
  vars: 
    ansible_python_interpreter: "/usr/bin/python"
  roles:
    - role: get-the-sap-sid-adm-account