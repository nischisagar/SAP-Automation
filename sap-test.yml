---
- name: Execute db create commands
  hosts: "{{ hname1 }}"
  become: yes
  become_method: sudo
  gather_facts: yes
  roles:
    - Oracle_standalone_home_installation
    - Oracle_standalone_DB_creation
    - when: ansible_distribution | lower == "aix" 
    - ignore_errors: yes