---
- name: Collect SAP CRYPTO Version
  block:
    - name: Get the SID
      win_shell: |
        $Service=get-wmiobject win32_service |?{$_.Name -like '*SAP???_??*' -and $_.pathname -match 'D00|DVE|SCS' -and $_.state -eq "Running"}
        $sub_service=(($service.Name).Substring(3)).split("_")
        $con="adm"
        $sid1=$sub_service[0].ToLower()
        echo "$sid1$con" > C:\temp\sidlisttest1.txt
        $sid2=$sub_service[2].ToLower() 
        echo "$sid2$con" >> C:\temp\sidlisttest1.txt
        $sid3=$sub_service[4].ToLower() 
        echo "$sid3$con" >> C:\temp\sidlisttest1.txt
        $sid4=$sub_service[6].ToLower()
        echo "$sid4$con" >> C:\temp\sidlisttest1.txt
        Get-Content C:\temp\sidlisttest1.txt | Get-Unique | Set-Content C:\temp\sidlisttest2.txt
      register: sid
      ignore_errors: true

    - name: collect sid output
      win_shell: |
        cat C:\temp\sidlisttest2.txt
      register: sid_list

    - debug:
        msg: "{{ item }}"
      with_items: "{{ sid_list['stdout_lines'] }}"

    - name: Count number of users present
      win_shell: |
        $user_cnt=(Get-Content C:\temp\sidlisttest2.txt).Length
        for (( $lo = 3 ); $user_cnt -le $lo; $user_cnt++ )
        {
          echo "dummy" >> C:\temp\sidlisttest2.txt
        }
        cat C:\temp\sidlisttest2.txt
      register: sid_list1

    - debug:
        msg: "{{ item }}"
      with_items: "{{ sid_list1['stdout_lines'] }}"

      

    

