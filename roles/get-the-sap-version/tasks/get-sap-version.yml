---
- name: Get the SAP version 
  shell: "echo {{ item }}"
  loop:
    - tkeadm
    - tksadm
  register: get_version_of_sap_output
  changed_when: False
  args:
    warn: false
 
- debug:
    msg: "{{ get_version_of_sap_output }}"

