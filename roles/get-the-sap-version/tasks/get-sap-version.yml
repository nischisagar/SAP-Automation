---
- name: Get the SAP version 
  shell: "{{ sap_version_check_script }}"
  become_user: "{{ sid_adm_usr }}"
  become: true
  register: get_version_of_sap_output
  changed_when: False
  args:
    warn: false

- debug:
    msg: "{{ get_version_of_sap_output.stdout_lines }}"