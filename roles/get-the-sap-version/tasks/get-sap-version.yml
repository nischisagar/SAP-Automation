---
- name: Get the SAP version 
  shell: "echo {{ item.sid_adm_usr }}"
  loop:
    - { sid_adm_usr: 'tkeadm'}
    - { sid_adm_usr: 'tksadm'}
  register: get_version_of_sap_output
  changed_when: False
  args:
    warn: false
 
- debug:
    msg: "{{ item.get_version_of_sap_output.stdout_lines }}"
  loop:
    - { sid_adm_usr: 'tkeadm'}
    - { sid_adm_usr: 'tksadm'}